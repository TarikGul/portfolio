import { connect } from 'react-redux';

import BlogForm from './blog_form';

const msp = state => {
    return {

    };
};

const mdtp = dispatch => {
    return {
        
    };
};

export default connect(
    msp,
    mdtp
)(BlogForm)