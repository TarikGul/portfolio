import { connect } from 'react-redux';
import BlogMenu from './blog_menu';

const msp = state => {
    return {

    };
};

const mdtp = dispatch => {
    return {

    };
};

export default connect(
    msp,
    mdtp
)(BlogMenu);
