import { connect } from 'react-redux';

import Navbar from './navbar'

const msp = state => {
    return {

    };
};

const mdtp = dispatch => {
    return {

    };
};

export default connect(
    msp,
    mdtp
)(Navbar)