import { connect } from 'react-redux';
import Main from './main_page';

const msp = state => {
    return {

    };
};

const mdtp = dispatch => {
    return {

    };
};

export default connect(
    msp, 
    mdtp
)(Main);