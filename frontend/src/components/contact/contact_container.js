import { connect } from 'react-redux';

import Contact from './contact';

const msp = state => {
    return {

    };
};

const mdtp = dispatch => {
    return {
        
    };
};

export default connect(
    msp,
    mdtp
)(Contact);